# Pipeline-stage evolution — branching_tree @ res=2.0

`after_p3_p4_p5.png` shows the post-plan_07 mesh at three
pipeline stages (Folder → Mesher → Stitcher).

For the BEFORE version, see the per-case storyboard:
`compare/per_case/branching_tree/res_2.0/before_vs_after.png`
(top-left and bottom-left panels show the pre-plan_07 P5
mesh; the harness only saved P5, not P3/P4 separately).
